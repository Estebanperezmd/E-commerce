// RestaurantRepository define cómo obtener restaurantes.
//
// Métodos esperados:
//
// - listByCity(cityId: number): Promise<Restaurant[]>

export class RestaurantRepository {
  async listByCity(cityId) {
    throw new Error("RestaurantRepository.listByCity() no implementado");
  }
}